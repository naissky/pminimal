---
import { headerData } from '../data';
---

<header>
    <div class="flex flex-col">
        <a href="/" class="header-text text-xl hover:text-orange-400 transition-all duration-200 text-black inline">{headerData.title} 🚀</a>
        <span class="text-zinc-600 text-lg">{headerData.subtitle}</span>
    </div>
</header>

<style>

    .header-text {
        animation: header-text 0.8s ease-in-out;
    }

    @keyframes header-text {
        0% {
            filter: blur(2px);
            opacity: 0;
        }
        100% {
            filter: blur(0px);
            opacity: 1;
        }
    }
</style>
